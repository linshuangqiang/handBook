Array.push()
    给数组追加元素
结构
    array.push(value,...)
参数
    value...
        追加到数组尾部的一个或多个值
返回值
    返回追加后数组的长度
说明 
    push()会将数组按照顺序追加到array的尾部。会直接修改array，而不会新创建新数组。一般与pop方法组成一种结构，栈操作先进后出（FILO）
相关方法
    array.pop()