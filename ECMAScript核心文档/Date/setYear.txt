Date.setYear()
    设置一个Date的年份值
结构
    date.setYear(year)
参数
    一个用做Date对象date的年份值的整数，如果这个值在0-99之间，则会自动加上1900
返回
    调整后的日期的毫秒数，在ES标准之前，没有返回值
说明
    setYear()设置给定的Date对象的年份值，其中1900-1999之间的年份的行为有些特别，根据ES3标准，JavaScript实现中已不在对这个函数做要求，建议使用setFullYear方法来替代。